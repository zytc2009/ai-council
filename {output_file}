### 整体评价

“模型重要还是 harness 重要”这个问题，本质上不是二选一，而是系统瓶颈判断问题。模型决定能力上限，harness 决定能力兑现率；在真实工程里，后者经常被低估。只看模型，容易高估单次 demo 的表现；只看 harness，又会在复杂推理、泛化和边界场景上撞上天花板。我的判断是：如果模型已经过了可用阈值，harness 往往比继续换更强模型更能提升整体产出；但如果模型基础能力不够，harness 只能放大局部收益，无法根治核心缺陷。

### 关键要点

1. 模型决定“能不能做出来”，harness 决定“稳不稳、快不快、贵不贵地做出来”。
2. 在生产环境中，任务拆解、上下文管理、工具调用、重试机制、评测闭环这些 harness 能力，往往比模型榜单差距更影响最终效果。
3. 最常见的误判是把 harness 问题当成模型问题：其实很多失败不是模型不会，而是提示组织差、检索脏、状态管理乱、工具接口不稳定。

### 补充建议

建议用“分层诊断”而不是抽象争论来判断优先级。具体做法是：先固定一个任务集，区分失败原因是基础推理错误、知识缺失、工具调用失败、上下文丢失还是输出格式不稳定；如果大部分问题来自流程和稳定性，就优先投入 harness；如果大部分问题来自推理深度、长程依赖或复杂决策能力不足，再升级模型。更具体一点，可以把系统指标拆成四类：任务成功率、单位成本、时延、波动性，然后分别测试“换模型不换 harness”和“换 harness 不换模型”，用 ablation 而不是感觉做决策。